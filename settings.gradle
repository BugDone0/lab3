pluginManagement {
    repositories {
        maven {
            url = 'https://jitpack.io'
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'vulnerable-app'
include 'submodule-isolation-1'
include 'submodule-isolation-2'

